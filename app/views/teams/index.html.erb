<h3>Teams</h3> 

<% @teams.each do | team | %>
	<b><%= team.name %></b> 
	(<%= team.acronym %>) | 
	<%= team.city %>
	 - 
	<%= team.light_color %><br>
<% end %>

<br>
<%= link_to "Add a new team", new_team_path%>